# build directories (top-level and nested, e.g. examples/*/build/)
/build/
build/

# generated journal output from the example drivers
*.jsonl

# ...except the committed parity fixtures (frozen pre-v1 journals; see
# tests/parity/gen_fixtures.cpp)
!tests/parity/fixtures/*.jsonl

# ...and the committed viewer fixtures (v1 journals; see viewer/fixtures/README.md)
!viewer/fixtures/*/*.jsonl

# Python
__pycache__/
*.egg-info/
.venv/

